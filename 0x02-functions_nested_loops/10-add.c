#include <stdio.h>
#include "main.h"

/**
 * add - add two integers
 * @a : first integer
 * @b : second integer
 * Return result: returns the result of the addition
*/
int add(int a, int b)
{
int result;
result = a + b;
return (result);
}
