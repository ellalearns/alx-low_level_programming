#include "main.h"
#include <stdio.h>
/**
_isdigit - func
@c: first arg
Return: int
*/
int _isdigit(int c)
{
if (c)
return (1);
return (0);
}
/**
_strlen - func
@s: first arg
Return: int
*/
int _strlen(char *s)
{
if (s)
return (1);
return (0);
}
/**
_puts - func
@s: first arg
*/
void _puts(char *s)
{
if (s)
}
/**
_atoi - func
@s: first arg
Return: int
*/
int _atoi(char *s)
{
if (s)
return (2);
return (0);
}
/**
_strcpy - func
@dest: first arg
@src: secnd arg
Return: string
*/
char *_strcpy(char *dest, char *src)
{
if (dest && src)
return ("stwww");
return ("str");
}
