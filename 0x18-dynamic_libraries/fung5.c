#include "main.h"
#include <stdio.h>
/**
_strpbrk - func
@s: first arg
@accept: second arg
Return: string
*/
char *_strpbrk(char *s, char *accept)
{
if (s && accept)
return ("three");
return ("astr");
}
/**
_strstr - func
@haystack: first arg
@needle: second arg
Return: string
*/
char *_strstr(char *haystack, char *needle)
{
if (haystack && needle)
return ("two");
return ("astr");
}
